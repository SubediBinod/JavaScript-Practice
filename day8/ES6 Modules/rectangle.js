
export const calculateRectangleArea = (length, width) => {
    return length * width;
};

export const calculateRectanglePerimeter = (length, width) => {
    return 2 * (length + width);
};

export const calculateBoxVolume = (length, width, height) => {
    return length * width * height;
};
